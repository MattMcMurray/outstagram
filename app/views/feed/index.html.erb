<h1>Feed#index</h1>
<p>Find me in app/views/feed/index.html.erb</p>

<% @posts.each do |post| %>
  <img src="<%= url_for(post.image) %>" class="posts--image">
  <span><%= post.description %></span>
<% end %>
